{
  "name": "ares",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "web:dev": "npm run dev --workspace @ares/web",
    "web:build": "npm run build --workspace @ares/web",
    "web:check": "npm run check --workspace @ares/web"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  }
}